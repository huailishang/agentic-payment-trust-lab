# P1 授权绑定与执行前核验执行记录

> 日期：2026-07-30
> 状态：P1 Gate 通过（PASS）
> 范围：离线实验，不接真实支付、真实凭证或真实资金。

## 1. 已实现事实

新增 `trusted_execution/confirmation.py`：

- `ConfirmationRecord` 保存 confirmation、authority、授权订单引用、确认时 Hash、确认时间、过期时间和状态；
- `confirmation_order_payload()` 只投影用户确认的交易内容：merchant、payee、items、金额、币种、服务与履约条款；不把 order version 当作 Hash 的替代品；
- `verify_confirmation_binding()` 在付款前重新计算 Hash，返回 `VALID`、`INVALID` 或 `MISSING_EVIDENCE`，并记录 `reason` 与 `invalidated_by`；
- `execute_with_confirmation_gate()` 仅在 `VALID` 时调用支付回调。`INVALID` 映射为 `CONFIRMATION_REQUIRED`，证据缺失映射为 `INDETERMINATE`。

这保持了边界：可信执行域先给出绑定事实，支付域的闸门才映射动作；Hash 不被表述为防篡改存储、身份认证或生产支付安全。

## 2. 已验证行为

最终核心专项命令：

```powershell
D:\SoftWare\Anaconda\workspace\.conda\envs\agent\python.exe -m pytest -q tests\trusted_execution\test_confirmation.py tests\test_validator.py tests\test_runner.py --basetemp .pytest_tmp
```

结果：`32 passed, 4 subtests passed`。

P1 新增测试验证：相同确认内容允许执行；金额或数量变化使旧确认失效并且不调用支付回调；授权版本变化、确认过期、确认状态失效、确认记录或当前订单缺失均不能静默执行。订单版本标签单独变化不替代内容 Hash，也不会在内容未变时误杀。

完整回归使用显式 UTF-8 环境运行：

```powershell
$env:PYTHONPATH = "src"
$env:PYTHONUTF8 = "1"
$env:PYTHONIOENCODING = "utf-8"
D:\SoftWare\Anaconda\workspace\.conda\envs\agent\python.exe -m unittest discover -s tests -v
```

结果：`Ran 200 tests ... OK`。

正式入口 `run_experiment.py` 结果：S01—S13 `13/13 PASS`；M5 的 decision error、unsafe allow、false refusal、missed confirmation、overconfident decision 和 forbidden side effect 均为 0；内部冻结基线 PASS。冻结基线只新增 S09 的 P1 确认事实与预期证据代码。

## 3. 主链接入事实

P1 已进入正式数据路径：

1. `IntentMandate.authority_version` 与 `Order.authority_version_ref` 已向后兼容加入领域模型；
2. Scenario Loader 读取并校验确认记录声明 Hash 与已确认订单快照一致；
3. S08 保持确认阈值语义，S09 携带真实 `ConfirmationRecord` 并由 `validate_request()` 消费绑定事实；
4. 交互实验修改“用户确认订单”时同步重建确认快照，修改最终订单时不会改写旧确认；
5. ACP 对照、Attack Overlay、学习变体、结果卡和 M5 均保留确认记录与证据链。

## 4. 边界与下一步

P1 仍不声称确认记录不可篡改、完成用户/Agent 身份认证、抵御订单与记录同时被改写，或达到生产支付安全。当前闸门是离线确定性控制；真实支付执行仍未接入。

下一步进入 P2：把已确认 Authority → Order 继续绑定到 Payment Request / Payment Execution，重点回答“最终被执行的是否仍是刚才核验的那笔请求”，并保留执行前禁止副作用测试。不新增 S14，不重构主 UI。
