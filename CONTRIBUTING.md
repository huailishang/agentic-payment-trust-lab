# Contributing

感谢对 Agentic Payment Trust Lab 的关注。

## 贡献原则

本项目优先接受能够被**场景、外部挑战或明确失败案例**证明有必要的改动，而不是为了“功能完整”提前建设大而全的平台。

推荐的贡献流程：

```text
提出具体支付 / 可信执行问题
    ↓
增加或引用可复现样品
    ↓
先写失败测试
    ↓
实现最小能力
    ↓
专项测试 + 全量回归
    ↓
更新边界和来源说明
```

## 必须遵守的边界

- 不接真实支付、真实卡、真实客户数据、生产凭证或主网资产。
- `trusted_execution` 只返回确定性验证事实，不直接定义 `ALLOW / DENY / CONFIRMATION_REQUIRED / INDETERMINATE` 等支付业务政策。
- 新增外部协议、密码学、身份、签名、幂等或审计能力时，应记录来源、版本、许可证、运行要求和不确定性。
- 外部协议与 benchmark 应固定版本或提交，不自动跟随上游变化。
- 不提交无法确认再分发权限的第三方原始 PDF 或其他二进制资料。

## 测试

提交前请运行：

```powershell
$env:PYTHONPATH = "src"
python -m unittest discover -s tests -v
```

新增能力应同时包含正向与负向测试。涉及危险支付动作时，应明确验证禁止副作用没有发生。

## 文档

面向读者的文档以中文为主；代码标识符、类名、函数名、协议缩写、文件名和路径保留原始英文。

当前状态以 `docs/01_项目现状/项目中控.md` 为准，长期架构以 `docs/03_架构设计/` 为准。
