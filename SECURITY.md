# Security Policy

## 项目边界

本项目是离线实验与研究仓库，不是生产支付系统。

请不要在 Issue、Pull Request、样品或日志中提交：

- 真实银行卡号、支付 Token、客户身份信息或银行账户信息；
- 生产 API Key、访问令牌、私钥、助记词、证书私钥或主网资产；
- 可直接用于生产放行 / 拒绝、清算或资金划转的内部控制规则；
- 未获授权公开的第三方资料。

所有示例默认使用合成数据、本地离线环境和测试密钥。

## 公开仓库证据规则

本仓库的任务合同、执行报告、评估结论和可复现实验代码可以进入 Git；Evaluator/Executor 产生的原始运行证据默认只保存在本机。

- `docs/05_任务交接/**/evidence/` 为 local-only（仅本地），不得提交原始 `.log`、临时 JSON、运行快照或机器环境转储；
- 公开文档不得记录真实用户目录、本机绝对路径或本机工具安装路径，使用 `<REPO_ROOT>`、`<USER_HOME>`、`<LOCAL_SOFTWARE>` 等占位符；
- 需要长期保留的验证结果，应在 `REPORT.md` / `EVALUATOR_REVIEW.md` 中保存命令语义、退出码、关键指标和必要 Hash，而不是提交完整终端日志；
- 可公开的固定样例、协议快照和正式测试夹具仍按正常代码审查进入 Git，不受上述 raw evidence（原始证据）目录规则影响。

## 报告安全问题

如果问题涉及仓库自身可能泄露敏感信息、绕过实验边界或导致危险副作用，请优先使用 GitHub 的私密安全报告能力，而不是公开 Issue。

普通功能缺陷、测试失败、文档错误和实验设计讨论可以使用公开 Issue。

## 不作出的安全承诺

以下实验结果不能被解释为生产安全证明：

```text
测试 PASS
!= 生产安全

Hash / Signature / Binding VALID
!= 业务合法

Idempotency VALID
!= 允许再次扣款

声明标识一致
!= 真实身份认证
```

本项目不会为真实支付、真实资金或生产环境提供安全保证。
